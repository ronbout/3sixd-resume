import React from "react";

const CandidateObjective = props => {
  return (
    <section className="candidate-objective candidate-tab-section">
      <p>this is the candidate Objective info</p>
    </section>
  );
};

export default CandidateObjective;
