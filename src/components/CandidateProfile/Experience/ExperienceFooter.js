import React from "react";

const ExperienceFooter = props => {
  return (
    <div className="exp-footer profile-section-footer">
      <button className="profile-save-btn">Add Experience</button>
    </div>
  );
};

export default ExperienceFooter;
